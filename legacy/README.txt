Archived static portfolio (pre-Next.js mbk/).

Includes:
  index.html, assets/
  images/           — referenced by index.html (e.g. portfolio thumbnails)
  documents/       — Resume.pdf (linked from old HTML via gh-pages URLs)
  .vscode/          — Live Server port & other workspace bits for previewing this site

Open legacy/index.html in a browser (or use Live Server from this folder) so CSS/JS paths resolve.
